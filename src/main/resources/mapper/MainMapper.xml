<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="MainMapper">

    <!-- 메인 배너에 넣을 이미지 찾기 -->
	<select id="imgBanner" resultType="com.project.travel.vo.MainVO">
		SELECT Photo_Num FROM Photo_Border_test ORDER BY Photo_Views DESC limit 4
	</select>
	
    <!-- 포토갤러리 배너에 넣을 이미지 찾기 -->
	<select id="photoBanner" resultType="com.project.travel.vo.MainVO">
		SELECT Photo_Num FROM Photo_Border_test ORDER BY rand() limit 10
	</select>
	
	<!-- 공지사항 목록 조회 -->
	<select id="qnaList" resultType="com.project.travel.vo.MainVO">
		SELECT QnA_Subject FROM QnA_Border_test ORDER BY QnA_Num DESC limit 5
	</select>
	
</mapper>